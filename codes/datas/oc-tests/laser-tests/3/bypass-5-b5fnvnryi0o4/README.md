Output side only open, to check the output side filter

That means that bypass on input side is closed and input itself is also closed. Other
parameters stay as is, except this time we only test with one valve opening value (only i0o4 not i0o{0,3,4})